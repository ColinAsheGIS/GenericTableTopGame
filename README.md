# Generic Table Top Game API
Using a table top game as context for pushing some GOF design patterns.
It doesn't do much yet...

# Tests
To run tests, simply `pip install -e .` in root dir && `pytest .`
